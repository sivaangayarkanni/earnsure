# EarnSure Backend

Express API with:

- `dotenv` config (`.env`)
- Winston logging (`utils/logger.js`)
- Health check endpoint: `GET /health`

## Run (local)

1. `cp .env.example .env`
2. `npm install`
3. `npm run dev`

