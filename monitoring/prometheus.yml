global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alert.rules.yml

scrape_configs:
  - job_name: "backend-services"
    metrics_path: /metrics
    static_configs:
      - targets: ["backend-services:3001"]
