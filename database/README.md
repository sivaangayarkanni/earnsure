# EarnSure Database

This folder is the home for PostgreSQL assets:

- `schema.sql`: bootstrap schema (used by `docker-compose.yml` for local dev)
- `migrations/`: forward-only migrations (recommended: Flyway / Liquibase / Prisma / knex)
- `seeds/`: seed data scripts

